server:
  requestLog:
      appenders:
      - type: "console"
  applicationConnectors:
  - type: "http"
    port: ${env.PORT}
  adminConnectors:
  - type: "http"
    port: ${env.PORT}

# Logging settings.
logging:
  level: INFO
  loggers:
    com.lowtuna.gymclasscal: DEBUG
  appenders:
  - type: "console"