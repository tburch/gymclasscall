server:
  requestLog:
      appenders:
      - type: "console"
  applicationConnectors:
  - type: "http"
    port: PORTPORT
  adminConnectors:
  - type: "http"
    port: PORTPORT

# Logging settings.
logging:
  level: INFO
  loggers:
    com.lowtuna.gymclasscal: DEBUG
  appenders:
  - type: "console"